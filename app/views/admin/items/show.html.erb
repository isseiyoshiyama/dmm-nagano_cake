<div class="col-md-8 mx-auto">
  <div class ="row mb-3">
      <h4 class="text-center bg-light">商品詳細</h4>
    </div>

  <div class="row">
    <div class="col-md-4"><%= attachment_image_tag @item, :image, :fill, 300, 300, format: 'jpeg' %></div>


    <div class="col-md-5">
      <table class ="table table-borderless">
          <tr>
            <th>商品名</th><td><%= @item.name %></td>
          </tr>
          <tr>
            <th>商品説明</th><td><%= @item.introduction %></td>
          </tr>
          <tr>
            <th>税込価格<br>(税抜価格)</th><td><%= (@item.price*1.1).floor %>（<%= @item.price %>）円</td>
          </tr>
          <tr>
            <th>販売ステータス</th><td><%= @item.is_active %></td>
          </tr>
      </table>
    </div>
  </div>

  <div class="text-center">
    <%= link_to "編集する", edit_admin_item_path(@item), class: 'btn btn-success btn-lg' %>
  </div>

</div>

